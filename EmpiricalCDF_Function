def ecdf(data):
    import numpy as np
    import matplotlib.pyplot as plt
    data=np.sort(data)
    leng=len(data)
    x=data
    y=np.arange(0, len(data))/data
    plt.xlabel('data')
    plt.ylabel('ecdf')
    plt.scatter(x,y)
    plt.show()
